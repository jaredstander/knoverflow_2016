<h4>Post an Answer Here!</h4>
<%= form_for [commentable, @comment], remote: true do |f| %>
  <%= f.label :content %>
  <br />
  <%= f.text_field :content, placeholder: "Your Comment" %>
  <br />
  <%= f.submit 'Submit Comment' %>
<% end %>