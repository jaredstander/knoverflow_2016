<div class="edit-form">
  <h1>Update Your Answer:</h1>
  <%= flash[:error] if flash[:error] %>
  <%= form_for [@question, @answer] do |f| %>
    <%= f.label :content %>
    <br />
    <%= f.text_field :content %>
    <br />
    <%= f.submit %>
  <% end %>
</div>